# 🔍 搜尋與過濾功能說明

## 新增功能概覽

Step2Save 現在支援強大的搜尋、過濾和排序功能，讓你更輕鬆地管理和查看存錢紀錄！

---

## ✨ 功能特色

### 1. **文字搜尋** 🔎
在專案詳情頁的「存錢腳印」區域，你可以：
- 搜尋備註內容
- 搜尋任務名稱
- 即時顯示搜尋結果

**使用方式：**
```
在「搜尋」欄位輸入關鍵字
→ 自動過濾符合的紀錄
```

---

### 2. **任務類型篩選** 📋
快速查看特定任務的所有完成紀錄。

**使用方式：**
```
在「任務」下拉選單選擇特定任務
→ 只顯示該任務的紀錄
```

**用途：**
- 查看「少喝咖啡」累計存了多少
- 統計某個任務的完成情況
- 分析不同任務的貢獻

---

### 3. **日期範圍篩選** 📅
查看特定時間段的存錢紀錄。

**使用方式：**
```
設定「開始日期」和「結束日期」
→ 只顯示該期間的紀錄
```

**實用場景：**
- 查看本週/本月存了多少
- 回顧上個月的成果
- 分析某段時間的存錢習慣

---

### 4. **智能排序** 🔢
4 種排序方式，快速找到你想看的紀錄。

| 排序方式 | 說明 | 適用場景 |
|---------|------|---------|
| **日期 (新→舊)** | 最新紀錄在前 | 查看最近的進度（預設） |
| **日期 (舊→新)** | 最舊紀錄在前 | 回顧存錢歷程 |
| **金額 (高→低)** | 金額大的在前 | 找出最大筆的成果 |
| **金額 (低→高)** | 金額小的在前 | 查看小額積累 |

---

## 🎯 使用範例

### 範例 1：查看本月咖啡省錢成果
```
1. 選擇任務：「少喝咖啡」
2. 設定日期範圍：2026/01/01 - 2026/01/31
3. 排序：金額 (高→低)
→ 看看這個月省了多少咖啡錢！
```

### 範例 2：搜尋帶圖片的紀錄
```
1. 搜尋：輸入「照片」或相關關鍵字
→ 找出所有有拍照記錄的成果
```

### 範例 3：查看最大筆存錢紀錄
```
1. 清除所有篩選
2. 排序：金額 (高→低)
→ 最驕傲的存錢時刻排在最前面！
```

---

## 🧭 UI 說明

### 篩選控制面板
位置：專案詳情頁的「存錢腳印 ✨」標題下方

```
┌─────────────────────────────────────────────┐
│ 篩選與排序                          清除篩選 │
│ ─────────────────────────────────────────── │
│ 顯示 5 / 20 筆紀錄                         │
│ ─────────────────────────────────────────── │
│                                             │
│ [搜尋] [任務] [開始日期] [結束日期]          │
│                                             │
│ 排序方式                                    │
│ [日期(新→舊)] [日期(舊→新)] ...              │
│                                             │
└─────────────────────────────────────────────┘
```

### 結果顯示
- **有結果**：顯示篩選後的卡片網格
- **無結果**：顯示「沒有符合條件的紀錄」+ 清除按鈕
- **計數器**：「顯示 X / Y 筆紀錄」

---

## 💡 實用技巧

### 技巧 1：組合篩選
多個條件可以同時使用！

```
✅ 任務 = "少喝咖啡" + 日期範圍 = 本週
→ 查看本週的咖啡省錢紀錄

✅ 搜尋 = "成功" + 排序 = 金額(高→低)
→ 找出備註有「成功」且金額最大的紀錄
```

### 技巧 2：快速重置
點擊右上角的「清除篩選」按鈕，一鍵恢復顯示全部。

### 技巧 3：只有紀錄時才顯示
如果還沒有任何存錢紀錄，篩選面板不會顯示，保持界面簡潔。

---

## 🔧 技術實現

### 新增文件

```
hooks/
└── useRecordFilters.ts          # 篩選邏輯 hook

components/
└── RecordFilters.tsx            # 篩選 UI 組件
```

### 核心功能

#### useRecordFilters Hook
```typescript
export function useRecordFilters(records: SavingRecord[]) {
  // 提供：
  // - filters: 當前篩選條件
  // - filteredRecords: 篩選後的結果
  // - updateFilter: 更新篩選條件
  // - resetFilters: 重置所有篩選
  // - hasActiveFilters: 是否有啟用的篩選
}
```

#### 篩選邏輯
1. **文字搜尋**：在備註和任務名稱中搜尋（不區分大小寫）
2. **任務篩選**：比對 taskId
3. **日期範圍**：比對 date 字段（包含開始/結束日）
4. **排序**：根據日期或金額排序

#### 效能優化
- 使用 `useMemo` 快取篩選結果
- 只在條件變更時重新計算
- React.memo 包裝組件避免不必要渲染

---

## 📊 Bundle 影響

| 項目 | 增加量 |
|------|--------|
| Bundle 大小 | +5KB (232KB → 237KB) |
| Gzip 大小 | +1KB (71KB → 72KB) |
| 新增組件 | 1 個 (RecordFilters) |
| 新增 Hook | 1 個 (useRecordFilters) |

**影響評估**：✅ 非常輕量，幾乎無感

---

## 🎨 設計亮點

### 1. 漸進式顯示
- 沒有紀錄 → 不顯示篩選面板
- 有紀錄 → 顯示完整篩選功能

### 2. 即時反饋
- 輸入搜尋詞 → 即時過濾
- 變更條件 → 即時更新結果數量
- 無結果 → 友善提示 + 快速清除

### 3. 視覺一致性
- 使用專案主題色
- 與現有設計語言一致
- 響應式佈局（手機/平板/桌面）

---

## 🚀 未來擴展可能

以下功能可以基於現有架構輕鬆添加：

### 擴展 1：保存篩選條件
```typescript
// 記住用戶常用的篩選組合
localStorage.setItem('favorite_filter', JSON.stringify(filters));
```

### 擴展 2：更多排序選項
- 按任務類型
- 按星期幾
- 隨機排序

### 擴展 3：進階搜尋
- 金額範圍（例如：$50-$100）
- 正則表達式搜尋
- 標籤系統

### 擴展 4：統計整合
```typescript
// 在篩選結果上顯示統計
totalAmount: filteredRecords.reduce((sum, r) => sum + r.amount, 0)
averageAmount: totalAmount / filteredRecords.length
```

---

## 📝 總結

搜尋與過濾功能讓 Step2Save 更加強大和實用：

✅ **易用性**：直觀的 UI，無需學習
✅ **靈活性**：多條件組合，滿足各種需求
✅ **效能**：高效篩選，即時響應
✅ **擴展性**：架構清晰，易於添加新功能

現在你可以更有效地管理和分析你的存錢紀錄了！🎉

---

**功能版本**: v2.1
**完成日期**: 2026-01-12
**下一步**: 準備添加 PWA 支持
